version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 10

    commands:
      - npm install

  build:
    commands:
      - npm run clean
      - npm run dev
      - npm run prod
      - echo Finalizou OK

artifacts:
  discard-paths: yes
  base-directory: dist/
  files:
    - rhino.css
    - rhino.min.css